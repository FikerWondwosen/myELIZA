#ifndef CONVERSATION_H
#define CONVERSATION_H

#include <string>
#include <iostream>
#include <vector>
#include <regex>

using namespace std;

//string getResponse(string input , vector<string> prevInput);



string genericResp(const string& text); //used for responding to generic conversations like travel, purchase, etc..

// string relationshipResp(const string& text); //used for responding to relationship conversations like love, boyfriend , girlfriend

// string concernResp(const string& text); //used for responding to a concerning conversations such as hate, kill, etc...

// string financialResp(const string& text); //used for responding to a financial conversations like money, saving, etc...

// string wellbeingResp(const string& text); //used for responding to conversations that include wellbeing word such as exercise

// string educationResp(const string& text); //used for responding to education related conversations

// string entertainmentResp(const string& text); //used for responding to entertainment related conversations

// string otherResp(const string& text); //handles all the other conversations that isn't handled by the other functions



#endif






























